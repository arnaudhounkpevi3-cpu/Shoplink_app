{
  "name": "shoplink-app-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "vercel-install": "npm install --prefix backend && npm install --prefix frontend",
    "vercel-build": "npm run build --prefix frontend"
  }
}
